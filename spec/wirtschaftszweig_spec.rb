require 'spec_helper'

describe Wirtschaftszweig do
  it 'has a version number' do
    expect(Wirtschaftszweig::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
